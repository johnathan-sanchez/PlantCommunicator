specVersion: "v1beta"
serviceId: "backend"
location: "us-east4"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
